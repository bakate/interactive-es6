// === Promesses : Construis toi-même ta promesse

function getEventualValue (delay, value) {
  // ???
}

// === Code de vérification -- ne pas toucher

const started = Date.now()
assertResolvesTo(
  getEventualValue(50, 42),
  42
)
